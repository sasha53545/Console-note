{
  "name": "console-note",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "babel . --out-dir .compiled --source-maps --ignore node_modules,.compiled",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "babel": {
    "presets": ["env"]
  },
  "bin": {
    "console-note": "index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^3.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
